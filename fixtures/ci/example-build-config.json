{
  "buildId": "example-build",
  "maxBuildDurationSec": 1800,
  "maxToolCalls": 80,
  "decisions": {
    "default": "always-approve",
    "rules": [
      {
        "pattern": "deploy",
        "action": "always-reject",
        "select": "Reject"
      }
    ]
  }
}
